# 笔记本 Agent 头像

将头像图片放入此目录，每个笔记本会分配不同头像（同课程内不重复）。

**必须提交到 git 并部署**，否则线上会 404，只能显示 DiceBear 占位头像。

**文件名**（与后端一致）：
- `098c1ecf70d121464cad33f41b7ef899.avif`（默认）
- `avatar1.avif` ~ `avatar10.avif`

**格式**：`.avif`、`.png`、`.jpg`、`.jpeg`、`.webp`

**操作**：将头像文件复制到此目录并重命名，然后 `git add` 并 `git push`。
